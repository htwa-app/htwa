/**
 * utils/authMessages.ts
 *
 * Shared user-facing auth error copy — was independently defined in both
 * app/login-email.tsx and app/verify.tsx, risking the wording drifting out
 * of sync between the two screens.
 */

export const NO_ACCOUNT_MESSAGE = "We couldn't find an account for that email.";
