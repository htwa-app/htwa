# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env.*
.env*.local
!.env.example
# 1Password op-run reference file (pointers only, but keep it local)
.secrets.env

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# Generated / local artifacts (were accidentally committed — untracked 31 May)
coverage/
supabase/.temp/
.claude/launch.json
.claude/worktrees/

# Google Play service account key (never commit)
google-service-account.json

# iCloud Drive conflict copies (Documents is synced) — never commit
*\ 2.*
*\ 3.*

# EAS submission credentials — Apple ASC API key (*.p8 already covered above);
# Play service-account JSON already covered above. Listed here for clarity.
